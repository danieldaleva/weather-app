import { StyleSheet } from 'react-native';
import * as appConstants from 'app/constants';

export const styles = StyleSheet.create({
  container: {},
  default: {},
  child: {
    padding: appConstants.CHILDREN_PADDING,
  },
});
