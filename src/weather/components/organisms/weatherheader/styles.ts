import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    minHeight: 150,
    maxHeight: 150,
    flexDirection: 'column',
  },
  weatherDate: {
    paddingLeft: 5,
  },
  weatherLocation: {
    marginTop: 10,
    paddingBottom: 5,
  },
  weatherDetail: {
    paddingLeft: 5,
    marginTop: 5,
  },
});
