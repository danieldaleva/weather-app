import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  image: {
    width: 200,
    height: 200,
    marginTop: -30,
  },
  mainTemp: {
    maxHeight: 164,
  },
});
